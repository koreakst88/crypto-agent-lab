import type { IndicatorDefinition } from "../../../core/domain";

export const rsiDefinition: IndicatorDefinition = Object.freeze({
  definitionId: "rsi-close",
  version: "1",
  outputKind: "numeric",
});
