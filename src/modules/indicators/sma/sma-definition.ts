import type { IndicatorDefinition } from "../../../core/domain";

export const smaDefinition: IndicatorDefinition = Object.freeze({
  definitionId: "sma-close",
  version: "1",
  outputKind: "numeric",
});
