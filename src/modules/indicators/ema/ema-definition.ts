import type { IndicatorDefinition } from "../../../core/domain";

export const emaDefinition: IndicatorDefinition = Object.freeze({
  definitionId: "ema-close",
  version: "1",
  outputKind: "numeric",
});
